const hubConnection=(new signalR.HubConnectionBuilder).withUrl(HubUrl).configureLogging(signalR.LogLevel.Information).build();$(document).ready(function(){hubConnection.on("Send",function(n,t,i,r,u){toastr.info(`<div class="cursor-pointer" onclick="location.href = '/Dialog/WithId/${r}'">
	                        <b>Новое сообщение!</p>
	                        <div class="d-flex">
		                        <div style="background-image: url('${u}'); background-size:cover; background-position:center; width: 40px; height: 40px; border-radius: 100%;"></div>
		                        <div class="ml-2">
			                        <b>${t}</b>
			                        <p class="mb-0 pb-0">${n}</p>
		                        </div>
	                        </div>
                        </div>`)});hubConnection.start()});hubConnection.on("Error",function(n){toastr.error(n)});